import React from "react";
import { BOARD_ARR } from "./gameInfo";

const Player1SetupBoard = ({
  player1Ready,
  setPlayer1Ready,
  player1Cells,
  setPlayer1Cells,
  shipSelected,
  axisShipDirection,
  carrierRef,
  battleShipRef,
  cruiserRef,
  submarineRef,
  destroyerRef,
}) => {
  return (
    <div>
      <h1>Player1SetupBoard</h1>
    </div>
  );
};

export default Player1SetupBoard;
